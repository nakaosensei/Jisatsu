Essa seção contém os códigos que realizam a busca e coleta nos sites Gbif, Specieslink e FloradoBrasil,
esses scripts lem um arquivo xls e realizam a busca pelas ocorrências(Gbif e Specieslink).

Na terceira entrega, os scripts de busca de ocorrências do Gbif e Specieslink foram entregues.

Os códigos a serem executados são GbifRequests.py e SpecieslinkRequests.py, com uma chamada pelo terminal
python3 GbifRequests, por exemplo.

Adicionalmente, ambos os scripts requerem que o banco de dados do Mysql esteja ativo. o script do banco está no diretório  ../database.sql

Os arquivos de testes são:
testsDecoder.py
testsGbifRequests.py
testsPlanilha.py
TestsSpeciesLinkRequests.py

Para testá-los basta rodar o comando:
python3 -m unittest <nomeTeste.py>
